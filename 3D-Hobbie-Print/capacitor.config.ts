import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '3D-Hobbie-Print',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
